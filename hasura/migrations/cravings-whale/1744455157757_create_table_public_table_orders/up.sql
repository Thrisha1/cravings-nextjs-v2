CREATE TABLE "public"."table_orders" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "user_id" uuid NOT NULL, "partner_id" uuid NOT NULL, "table_number" integer NOT NULL, "menu_items" jsonb NOT NULL, "ordered_at" date NOT NULL, "total_amount" integer NOT NULL, "discount" integer NOT NULL, "waiting_time" date NOT NULL, "delivery_status" text NOT NULL, "payment_status" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("partner_id") REFERENCES "public"."partners"("id") ON UPDATE restrict ON DELETE restrict);
CREATE EXTENSION IF NOT EXISTS pgcrypto;
